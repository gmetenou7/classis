const defaultsInputClasses = {
  input: "!h-[50px] !text-xl",
  label: "!text-xl !w-full",
};

const defaultsTextareaClasses = {
  input: "!h-[150px] !text-xl",
  label: "!text-xl !w-full",
};

export { defaultsInputClasses, defaultsTextareaClasses };
